import { Shop } from "./shop";
describe("Shop", () => {
  it("adds item and returns price", () => {
    // const shop = new Shop();
    // shop.addItem("A", 50);
    // expect(shop.checkout("A")).toBe(50);
  });
});
